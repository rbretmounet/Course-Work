#ifndef FUNCTIONS_HPP
#define FUNCTIONS_HPP

#include <iostream>
#include "bus.hpp"
using namespace std;

//Prototypes
void printBusses(Bus []);
void addMaintenanceRecord(Bus[]);
void displayMaintHist(Bus[]);
void compareBusses(Bus[]);
void copyBus(Bus[]);

#endif
